/**
 * The width every wellbeing page centres, as a `max-w-*` class for `Frame`.
 *
 * Every wellbeing page is this wide — the questionnaire, the record, Patterns
 * and Questions alike — so the column is the one place the half's width is
 * decided, and every tab sits centred with its heading at one x.
 */
export const COLUMN = 'max-w-6xl'
