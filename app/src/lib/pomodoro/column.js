/**
 * The width every focus page centres, as a `max-w-*` class for `Frame`.
 *
 * The timer and its patterns both fill it; neither page carries a width of its
 * own.
 */
export const COLUMN = 'max-w-3xl'
