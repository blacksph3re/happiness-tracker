/**
 * The width every time page centres, as a `max-w-*` class for `Frame`.
 *
 * Every time page is this wide — Track, Record, Projects and Patterns alike —
 * so the column is the one place the half's width is decided, and every tab
 * sits centred with its heading at one x.
 */
export const COLUMN = 'max-w-5xl'
